- Create examples of basic functions that represent the diferents possibilities of the lenguage:
    Whitout return, with one or varius parameters, and with returns.
- Try to create a functions inside a function.
- Use son functions created in the lenguage.
- Use any example of functions created in the lenguage.
- Use the concept of local variable and global variable.
- Print in console the results of the examples.

Extra:
- Create a function that take 2 parameters of string and return a number
- The function print all numbers between 1 to 100 with the next instructions:
    If the number is multiple of 3, show the string of the first parameter.
    If the number is multiple of 5, show the string of the second parameter.
    If the number us multiple of 3 and 5, show the two strings concat
    The function print the times of prints the number insted de texts
