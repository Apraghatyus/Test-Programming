services:
  ts-dev:
    build: .
    working_dir: /app
    volumes:
      - .:/app
      # node_modules vive dentro del contenedor, no en tu máquina
      - node_modules:/app/node_modules
    tty: true
    stdin_open: true

volumes:
  node_modules: